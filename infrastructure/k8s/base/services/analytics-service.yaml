apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: rajutechie-streamkit
  labels:
    app.kubernetes.io/name: analytics-service
    app.kubernetes.io/part-of: rajutechie-streamkit-platform
    app.kubernetes.io/component: service
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: analytics-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: analytics-service
        app.kubernetes.io/part-of: rajutechie-streamkit-platform
        app.kubernetes.io/component: service
    spec:
      containers:
        - name: analytics-service
          image: rajutechie-streamkit/analytics-service:latest
          ports:
            - containerPort: 3019
              protocol: TCP
          envFrom:
            - configMapRef:
                name: rajutechie-streamkit-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3019
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3019
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: rajutechie-streamkit
  labels:
    app.kubernetes.io/name: analytics-service
    app.kubernetes.io/part-of: rajutechie-streamkit-platform
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: analytics-service
  ports:
    - port: 3019
      targetPort: 3019
      protocol: TCP
      name: http
