apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: rajutechie-streamkit
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/part-of: rajutechie-streamkit-platform
    app.kubernetes.io/component: gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: api-gateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: api-gateway
        app.kubernetes.io/part-of: rajutechie-streamkit-platform
        app.kubernetes.io/component: gateway
    spec:
      containers:
        - name: api-gateway
          image: rajutechie-streamkit/api-gateway:latest
          ports:
            - containerPort: 3000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: rajutechie-streamkit-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: rajutechie-streamkit
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/part-of: rajutechie-streamkit-platform
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: api-gateway
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
