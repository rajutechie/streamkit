apiVersion: apps/v1
kind: Deployment
metadata:
  name: ws-gateway
  namespace: rajutechie-streamkit
  labels:
    app.kubernetes.io/name: ws-gateway
    app.kubernetes.io/part-of: rajutechie-streamkit-platform
    app.kubernetes.io/component: gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: ws-gateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ws-gateway
        app.kubernetes.io/part-of: rajutechie-streamkit-platform
        app.kubernetes.io/component: gateway
    spec:
      containers:
        - name: ws-gateway
          image: rajutechie-streamkit/ws-gateway:latest
          ports:
            - containerPort: 3001
              protocol: TCP
          envFrom:
            - configMapRef:
                name: rajutechie-streamkit-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: ws-gateway
  namespace: rajutechie-streamkit
  labels:
    app.kubernetes.io/name: ws-gateway
    app.kubernetes.io/part-of: rajutechie-streamkit-platform
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  selector:
    app.kubernetes.io/name: ws-gateway
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: ws
