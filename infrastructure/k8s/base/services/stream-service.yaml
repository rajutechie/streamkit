apiVersion: apps/v1
kind: Deployment
metadata:
  name: stream-service
  namespace: rajutechie-streamkit
  labels:
    app.kubernetes.io/name: stream-service
    app.kubernetes.io/part-of: rajutechie-streamkit-platform
    app.kubernetes.io/component: service
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: stream-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: stream-service
        app.kubernetes.io/part-of: rajutechie-streamkit-platform
        app.kubernetes.io/component: service
    spec:
      containers:
        - name: stream-service
          image: rajutechie-streamkit/stream-service:latest
          ports:
            - containerPort: 3015
              protocol: TCP
          envFrom:
            - configMapRef:
                name: rajutechie-streamkit-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3015
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3015
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: stream-service
  namespace: rajutechie-streamkit
  labels:
    app.kubernetes.io/name: stream-service
    app.kubernetes.io/part-of: rajutechie-streamkit-platform
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: stream-service
  ports:
    - port: 3015
      targetPort: 3015
      protocol: TCP
      name: http
